---
import Glyph from "./Glyph.astro";
import { type Glyph as TGlyph } from "../../lib/data/glyphs";

interface Props {
    glyphs: TGlyph[]
}

const { glyphs } = Astro.props;
---
<Fragment>
    {glyphs.map(glyph => <Glyph classes="me-1" glyph={glyph} />)}
</Fragment>
